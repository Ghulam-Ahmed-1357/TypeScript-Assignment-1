"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
let usernames = [
    "Admin",
    "Ahmed",
    "Uzair",
    "Ali",
    "Rameez",
    "Hamza",
    "Faaz",
];
if (usernames.length == 0) { // false
    console.log("We need to find some users!");
}
usernames = [];
if (usernames.length == 0) { // true
    console.log("We need to find some users!");
}
